@model IEnumerable<OnlineExamManagement.Models.Db.Questions>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Question)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TopicsCodeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Question)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TopicsCodeNavigation.TopicsCode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QuestionNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QuestionNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QuestionNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
