@model IEnumerable<OnlineExamManagement.Models.Db.Answers>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Answer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Correct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuestionNoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Answer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Correct)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionNoNavigation.QuestionNo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AnswerSerial">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AnswerSerial">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AnswerSerial">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
